
import './Header.css';

export default function Header(props) {
    return (

        <header className="Header">
            <img src="./Logo.png" alt="monkeylogo" className="logo" />
            {/* Click the Skull (Murray) to get started */}
        </header>
    );
}