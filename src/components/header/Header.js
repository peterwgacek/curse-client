
import './Header.css';

export default function Header(props) {
    return (

        <header className="Header">
            Welcome to:
            Click the Skull (Murray) to get started
        </header>
    );
}